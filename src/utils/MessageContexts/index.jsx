import { createContext } from "react";

/**
 * A context object for managing messages in the application.¨
 * @example
 * <MessageContext.Provider value={message}>
 */

const MessageContext = createContext();

export default MessageContext;
